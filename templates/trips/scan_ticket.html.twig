{% extends 'base.html.twig' %}

{% block title %}Verifică Bilet{% endblock %}

{% block content %}
    <div class="container mt-5">
        <h2 class="mb-4 text-center">Verifică Bilet</h2>

        <div class="row justify-content-center mb-4">
            <div class="col-md-6">
                <form id="qrForm" method="get" class="d-flex">
                    <input type="text" id="qrInput" name="qr_code" class="form-control me-2" placeholder="Introdu codul QR" value="{{ qrCode|default('') }}">
                    <button type="submit" class="btn btn-primary me-2">Caută</button>
                    <button type="button" id="scanBtn" class="btn btn-success">Scan QR</button>
                </form>
            </div>
        </div>

        <div id="reader" class="mb-3" style="width:100%; max-width:300px; margin:auto; display:none;"></div>

        {% if qrCode is defined and qrCode is not empty %}
            <div class="row justify-content-center">
                <div class="col-md-6">
                    {% if valid is defined and valid %}
                        <div class="alert alert-success text-center" role="alert">
                            Bilet valid! 🎉
                        </div>
                    {% else %}
                        <div class="alert alert-danger text-center" role="alert">
                            Bilet invalid! ❌
                        </div>
                    {% endif %}
                </div>
            </div>
        {% endif %}
    </div>
{% endblock %}

{% block javascripts %}
    <script src="https://unpkg.com/html5-qrcode/minified/html5-qrcode.min.js"></script>
    <script>
        const scanBtn = document.getElementById('scanBtn');
        const qrInput = document.getElementById('qrInput');
        const qrForm = document.getElementById('qrForm');
        const readerDiv = document.getElementById('reader');
        let html5QrCode;

        scanBtn.addEventListener('click', () => {
            readerDiv.style.display = "block";
            html5QrCode = new Html5Qrcode("reader");

            html5QrCode.start(
                { facingMode: "environment" },
                {
                    fps: 10,
                    qrbox: 250
                },
                qrCodeMessage => {
                    qrInput.value = qrCodeMessage;
                    html5QrCode.stop().then(() => {
                        readerDiv.style.display = "none";
                        qrForm.submit();
                    }).catch(err => {
                        console.error("Failed to stop the scanner: ", err);
                    });
                },
                errorMessage => {
                    console.log(errorMessage);
                }
            ).catch(err => {
                console.error(err);
            });
        });
    </script>
{% endblock %}